'use client';

import { useCallback, useEffect, useMemo, useState } from 'react';
import api from '@/lib/api-client';

type Kind = 'discount' | 'banner';
type Creative = { desktop_url?: string; title?: string; description?: string; alt_text?: string; cta_text?: string };
type Campaign = { id: number; name: string; description?: string; status: string; approval_status?: string; rejection_reason?: string; scope?: string; discount_type?: string; discount_value?: number; starts_at: string; ends_at: string; schedule_timezone?: string; priority?: number; placement?: string; creatives?: Creative[]; analytics?: Record<string, number | string | null> };
type Audit = { id: number; action: string; reason?: string; created_at: string; snapshot: string };

const placements = ['homepage_hero', 'homepage_featured', 'category_page', 'store_page', 'sidebar'];
const label = (value?: string | null) => (value || '—').replaceAll('_', ' ').replace(/\b\w/g, character => character.toUpperCase());
const browserTimezone = () => Intl.DateTimeFormat().resolvedOptions().timeZone || 'UTC';
function instant(value: string | Date) {
  if (value instanceof Date) return value;
  const normalized = value.trim().replace(' ', 'T');
  return new Date(/(?:Z|[+-]\d{2}:?\d{2})$/i.test(normalized) ? normalized : `${normalized}Z`);
}
function schedule(value?: string, timezone = browserTimezone()) { if (!value) return '—'; try { return `${new Intl.DateTimeFormat(undefined, { dateStyle: 'medium', timeStyle: 'short', timeZone: timezone }).format(instant(value))} (${timezone})`; } catch { return `${instant(value).toLocaleString()} (${timezone})`; } }
function errorText(error: unknown) { const data = (error as { response?: { data?: { message?: string; errors?: Record<string, string[]> } } })?.response?.data; return data?.errors ? Object.values(data.errors).flat().join(' ') : data?.message || 'The request could not be completed.'; }
function Button({ children, onClick, tone = 'secondary', disabled = false }: { children: React.ReactNode; onClick?: () => void; tone?: 'primary' | 'secondary' | 'danger' | 'ghost'; disabled?: boolean }) { const styles = { primary: 'bg-blue-700 text-white hover:bg-blue-800', secondary: 'border border-slate-300 bg-white text-slate-700 hover:bg-slate-50', danger: 'bg-rose-600 text-white hover:bg-rose-700', ghost: 'text-blue-700 hover:bg-blue-50' }; return <button type="button" disabled={disabled} onClick={onClick} className={`rounded-lg px-3.5 py-2 text-sm font-semibold transition disabled:cursor-not-allowed disabled:opacity-45 ${styles[tone]}`}>{children}</button>; }
function Modal({ title, children, onClose }: { title: string; children: React.ReactNode; onClose: () => void }) { return <div className="fixed inset-0 z-[100] flex items-end justify-center bg-slate-950/45 sm:items-center sm:p-6" role="dialog" aria-modal="true" aria-label={title}><section className="max-h-[94vh] w-full max-w-6xl overflow-y-auto rounded-t-2xl bg-white shadow-2xl sm:rounded-2xl"><header className="sticky top-0 z-10 flex items-center justify-between border-b bg-white px-5 py-4 sm:px-7"><div><h2 className="text-xl font-bold text-slate-900">{title}</h2><p className="mt-1 text-sm text-slate-500">Dates and audit events are shown in your current browser timezone.</p></div><Button tone="ghost" onClick={onClose}>Close</Button></header><div className="p-5 sm:p-7">{children}</div></section></div>; }

export default function CampaignManager({ kind }: { kind: Kind; role?: 'admin' }) {
  const banner = kind === 'banner'; const base = `/admin/${kind}-campaigns`;
  const [rows, setRows] = useState<Campaign[]>([]); const [page, setPage] = useState(1); const [lastPage, setLastPage] = useState(1); const [status, setStatus] = useState(''); const [approval, setApproval] = useState(''); const [placement, setPlacement] = useState(''); const [search, setSearch] = useState(''); const [busy, setBusy] = useState(false); const [error, setError] = useState(''); const [notice, setNotice] = useState(''); const [details, setDetails] = useState<Campaign | null>(null); const [audits, setAudits] = useState<Audit[]>([]); const [usage, setUsage] = useState<{ id: number; order_id: number; units: number; revenue: number; discount_amount: number }[]>([]); const [reason, setReason] = useState('');
  const load = useCallback(async () => { setBusy(true); try { const response = await api.get(base, { params: { page, status: status || undefined, approval_status: approval || undefined, placement: placement || undefined, search: search || undefined } }); setRows(response.data.data.data); setLastPage(response.data.data.last_page); } catch (requestError) { setError(errorText(requestError)); } finally { setBusy(false); } }, [approval, base, page, placement, search, status]);
  useEffect(() => { void load(); }, [load]);
  const inspect = async (campaign: Campaign) => { setBusy(true); setError(''); try { const [detail, audit, metric] = await Promise.all([api.get(`${base}/${campaign.id}`), api.get(`${base}/${campaign.id}/audits`), api.get(`${base}/${campaign.id}/analytics`)]); setDetails(detail.data.data); setAudits(audit.data.data.data); setUsage(metric.data.data.usage?.data ?? []); setReason(''); } catch (requestError) { setError(errorText(requestError)); } finally { setBusy(false); } };
  const actions = useMemo(() => (campaign: Campaign) => { if (banner) { if (campaign.approval_status === 'pending') return campaign.status === 'paused' ? ['approve', 'reject', 'delete'] : ['approve', 'reject', 'pause', 'delete']; if (campaign.status === 'paused' && campaign.approval_status === 'approved') return ['resume', 'delete']; if (['active', 'scheduled'].includes(campaign.status)) return ['pause', 'delete']; return ['delete']; } if (campaign.status === 'paused') return ['resume', 'delete']; if (['active', 'scheduled'].includes(campaign.status)) return ['pause', 'delete']; return ['delete']; }, [banner]);
  const act = async (action: string) => { if (!details) return; setBusy(true); setError(''); try { await api.post(`${base}/${details.id}/actions`, { action, reason }); setNotice(`${label(action)} completed.`); setDetails(null); await load(); } catch (requestError) { setError(errorText(requestError)); } finally { setBusy(false); } };
  const quickAction = async (campaign: Campaign, action: string) => { const actionReason = action === 'reject' ? window.prompt('Give the seller a rejection reason:') ?? '' : ''; if (action === 'reject' && !actionReason.trim()) return; setBusy(true); setError(''); try { await api.post(`${base}/${campaign.id}/actions`, { action, reason: actionReason }); setNotice(`${label(action)} completed.`); await load(); } catch (requestError) { setError(errorText(requestError)); } finally { setBusy(false); } };
  const requiresReason = (action: string) => ['reject', 'terminate'].includes(action);
  return <div className="space-y-6 text-slate-900"><section className="rounded-2xl bg-gradient-to-r from-slate-950 via-slate-900 to-blue-950 p-6 text-white shadow-sm sm:p-8"><p className="text-sm font-semibold uppercase tracking-[0.16em] text-blue-200">Administration</p><h1 className="mt-2 text-3xl font-bold">{banner ? 'Promotional Banner Review' : 'Discount Campaign Control'}</h1><p className="mt-2 max-w-2xl text-slate-300">Review scheduled campaigns, inspect history and performance, and take controlled lifecycle actions.</p></section>{error && <div role="alert" className="rounded-xl border border-rose-200 bg-rose-50 p-4 text-rose-800">{error}</div>}{notice && <div role="status" className="rounded-xl border border-emerald-200 bg-emerald-50 p-4 text-emerald-800">{notice}</div>}<section className="rounded-2xl border border-slate-200 bg-white p-4 shadow-sm"><div className="flex flex-wrap gap-3"><input aria-label="Search campaigns" value={search} onChange={event => { setSearch(event.target.value); setPage(1); }} placeholder="Search campaigns" className="min-w-52 flex-1 rounded-lg border border-slate-300 px-3 py-2" /><select aria-label="Status" value={status} onChange={event => { setStatus(event.target.value); setPage(1); }} className="rounded-lg border border-slate-300 px-3 py-2"><option value="">All statuses</option>{['draft', 'scheduled', 'active', 'paused', 'expired', 'cancelled', 'completed'].map(value => <option key={value} value={value}>{label(value)}</option>)}</select>{banner && <><select aria-label="Approval" value={approval} onChange={event => { setApproval(event.target.value); setPage(1); }} className="rounded-lg border border-slate-300 px-3 py-2"><option value="">All approvals</option>{['pending', 'approved', 'rejected'].map(value => <option key={value} value={value}>{label(value)}</option>)}</select><select aria-label="Placement" value={placement} onChange={event => { setPlacement(event.target.value); setPage(1); }} className="rounded-lg border border-slate-300 px-3 py-2"><option value="">All placements</option>{placements.map(value => <option key={value} value={value}>{label(value)}</option>)}</select></>}</div></section><section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="overflow-x-auto"><table className="min-w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-5 py-4">Campaign</th><th className="px-5 py-4">Scope / placement</th><th className="px-5 py-4">Scheduled in your browser timezone</th><th className="px-5 py-4">Status</th><th className="px-5 py-4">Performance</th><th className="px-5 py-4 text-right">Review</th></tr></thead><tbody className="divide-y divide-slate-100">{rows.map(campaign => <tr key={campaign.id} className="hover:bg-slate-50/70"><td className="px-5 py-4"><p className="font-semibold">{campaign.name}</p><p className="mt-1 max-w-xs truncate text-slate-500">{campaign.description || 'No description'}</p></td><td className="px-5 py-4"><p>{label(campaign.scope || campaign.placement)}</p><p className="mt-1 text-xs text-slate-500">{banner ? `Approval: ${label(campaign.approval_status)}` : `${campaign.discount_value}${campaign.discount_type === 'percentage' ? '%' : ' fixed'} off`}</p></td><td className="px-5 py-4 text-slate-600"><p>{schedule(campaign.starts_at)}</p><p className="mt-1 text-xs">Ends {schedule(campaign.ends_at)}</p></td><td className="px-5 py-4"><span className="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold">{label(campaign.status)}</span></td><td className="px-5 py-4 text-slate-600">{campaign.analytics?.orders ?? campaign.analytics?.impressions ?? 0} events</td><td className="px-5 py-4 text-right"><div className="flex flex-wrap justify-end gap-2"><Button tone="ghost" onClick={() => void inspect(campaign)}>Details</Button>{actions(campaign).map(action => <Button key={action} tone={action === 'delete' || action === 'cancel' ? 'danger' : action === 'approve' ? 'primary' : 'secondary'} disabled={busy} onClick={() => void quickAction(campaign, action)}>{label(action)}</Button>)}</div></td></tr>)}</tbody></table></div>{!rows.length && <div className="p-10 text-center text-slate-500">{busy ? 'Loading campaigns…' : 'No campaigns match these filters.'}</div>}<footer className="flex items-center justify-between border-t border-slate-100 px-5 py-4"><span className="text-sm text-slate-500">Page {page} of {lastPage}</span><div className="flex gap-2"><Button disabled={page <= 1 || busy} onClick={() => setPage(value => value - 1)}>Previous</Button><Button disabled={page >= lastPage || busy} onClick={() => setPage(value => value + 1)}>Next</Button></div></footer></section>{details && <Modal title={`${details.name} — details, analytics & actions`} onClose={() => setDetails(null)}><div className="space-y-6"><section className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><div className="rounded-xl bg-slate-50 p-4"><p className="text-xs uppercase text-slate-500">Status</p><p className="mt-1 font-semibold">{label(details.status)}</p></div><div className="rounded-xl bg-slate-50 p-4"><p className="text-xs uppercase text-slate-500">Starts</p><p className="mt-1 font-semibold">{schedule(details.starts_at)}</p></div><div className="rounded-xl bg-slate-50 p-4"><p className="text-xs uppercase text-slate-500">Ends</p><p className="mt-1 font-semibold">{schedule(details.ends_at)}</p></div><div className="rounded-xl bg-slate-50 p-4"><p className="text-xs uppercase text-slate-500">{banner ? 'Approval' : 'Discount'}</p><p className="mt-1 font-semibold">{banner ? label(details.approval_status) : `${details.discount_value}${details.discount_type === 'percentage' ? '%' : ' fixed'} · priority ${details.priority}`}</p></div></section>{banner && details.creatives?.[0] && <section className="grid gap-4 sm:grid-cols-[240px_1fr]"><img src={details.creatives[0].desktop_url} className="max-h-48 w-full rounded-xl object-cover" alt={details.creatives[0].alt_text || ''} /><div><h3 className="font-bold">{details.creatives[0].title}</h3><p className="mt-2 text-slate-600">{details.creatives[0].description}</p><p className="mt-3 text-sm text-slate-500">CTA: {details.creatives[0].cta_text}</p></div></section>}<section><h3 className="mb-3 text-lg font-bold">Analytics</h3><div className="grid gap-3 sm:grid-cols-3">{Object.entries(details.analytics ?? {}).filter(([, value]) => typeof value === 'number').map(([key, value]) => <div key={key} className="rounded-xl border border-slate-200 p-4"><p className="text-xs uppercase text-slate-500">{label(key)}</p><p className="mt-1 text-xl font-bold">{value ?? 0}</p></div>)}</div></section>{usage.length > 0 && <section><h3 className="mb-3 text-lg font-bold">Order usage</h3>{usage.map(item => <p className="border-b py-2 text-sm" key={item.id}>Order #{item.order_id} · {item.units} units · €{item.revenue} revenue · €{item.discount_amount} discount</p>)}</section>}<section><h3 className="mb-3 text-lg font-bold">Audit history</h3><div className="space-y-2">{audits.map(audit => { let snapshot: Partial<Campaign> = {}; try { snapshot = JSON.parse(audit.snapshot); } catch {} return <details className="rounded-xl border border-slate-200 p-3" key={audit.id}><summary className="cursor-pointer font-medium">Audit action: {schedule(audit.created_at)} · {label(audit.action)}</summary><p className="mt-2 text-sm text-slate-600">{audit.reason || 'No reason supplied.'}</p>{snapshot.starts_at && <p className="mt-1 text-sm text-slate-500">Campaign schedule: {schedule(snapshot.starts_at)} → {schedule(snapshot.ends_at)}</p>}</details>; })}</div></section><section className="border-t pt-5"><label className="grid gap-1.5 text-sm font-medium text-slate-700">Reason for review actions<textarea value={reason} onChange={event => setReason(event.target.value)} placeholder="Required for rejection or termination. Approval, pause, resume, and delete are immediately available." className="min-h-20 rounded-lg border border-slate-300 px-3 py-2.5" /></label><div className="mt-4 flex flex-wrap gap-2">{actions(details).map(action => <Button key={action} tone={action === 'delete' || action === 'cancel' || action === 'terminate' ? 'danger' : action === 'approve' ? 'primary' : 'secondary'} disabled={busy || (requiresReason(action) && !reason.trim())} onClick={() => void act(action)}>{label(action)}</Button>)}</div></section></div></Modal>}</div>;
}
